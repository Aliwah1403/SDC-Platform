# Supabase
.branches
.temp

# dotenvx
.env.keys
.env.local
.env.*.local

# Local-only migration archive kept while reconstructing authoritative history.
_migrations_legacy/

# Third-party secrets used only by the local Edge Function runtime.
functions/.env
