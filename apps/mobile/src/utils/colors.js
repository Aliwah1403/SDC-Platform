// Hemo design system colors
// Source of truth for all colors used in the app

export const colors = {
  // ── Brand ──────────────────────────────────────────────────────────────────
  burgundy: "#A9334D",        // Primary accent — buttons, selected states
  darkBurgundy: "#781D11",   // Illustration color, welcome gradient
  orange: "#F0531C",          // Secondary accent, primary CTA buttons

  // ── Neutrals ───────────────────────────────────────────────────────────────
  darkTeal: "#09332C",        // Primary text, icons (not for backgrounds/tints)
  cream: "#F8E9E7",           // Light backgrounds, text on dark
  beige: "#F8F4F0",           // Page/card background
  pinkBorder: "#F0E4E1",      // Card borders, dividers
  red: "#DC2626",             // Active tab indicator, error states

  // ── UI feedback ────────────────────────────────────────────────────────────
  textPrimary: "#09332C",     // Main text
  textSecondary: "#9CA3AF",   // Labels, subtitles, secondary text
  textMuted: "#C4A8A4",       // Placeholders, very subtle text

  // ── Backgrounds ────────────────────────────────────────────────────────────
  white: "#ffffff",
  cardBg: "#ffffff",
  pageBg: "#F8F4F0",

  // ── Tints (use these instead of rgba(9,51,44,...)) ─────────────────────────
  burgundyTint: "rgba(169,51,77,0.08)",   // Subtle burgundy background
  burgundyBorder: "rgba(169,51,77,0.15)", // Burgundy-tinted border
};
